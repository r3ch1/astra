packages:
  - "apps/*"
  - "packages/*"

# Aprovação de build scripts. sharp não é necessário (não usamos next/image);
# esbuild funciona via binário pré-compilado, então o build é dispensável.
allowBuilds:
  esbuild: false
  sharp: false
